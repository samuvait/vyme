/*import React from 'react';

const FlexGrid = ({ movies, windowSize }) => {

  const flexMovies = (movies) => {
    return(movies.map(movie => <FlexMovie movie={movie} />))
  }

  return (
    <FlexView column>
      {flexMovies(movies)}
    </FlexView>
  )
}

export default FlexGrid*/